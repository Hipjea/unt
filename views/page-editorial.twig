{% extends "base.twig" %}

{% block content %}
    <div class="container content-text">
        <div class="row mb-5">
            <div class="col">
                <h1 class="post-title">{{ post.post_title }}</h1>
                {{ post.content }}
            </div>
        </div>
    </div>
{% endblock %}