{% extends "base.twig" %}

{% block content %}
	<div class="container content-text post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<h1>{{ post.post_title }}</h1>
		<br/>
        {{ post.post_content }}
	</div>
{% endblock %}