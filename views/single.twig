{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<div class="content-text container">
			<div class="row">
				<article id="post-{{ news.id }}" class="news-post col-md-12 {% if latestPosts > 1 %} col-lg-8{% endif %}">
					<h1 class="article-heading">{{ news.title }}</h1>
					<div class="news-date">
						Publié le&nbsp;{{ news.date }}
						{% if categorie.slug == "actualites" %}
							dans les <a href="{{ site.link }}/liste-actualites/" class="link">actualités</a>
						{% endif %}
					</div>
					{{ news.fullContent }}
				</article>
				{% if latestPosts > 1 %}
					<aside class="news-siderbar d-none d-lg-inline col-lg-4">
						<h5>{{ appMessage[currentLang].latestPosts }}</h5>
						<ul>
							{% for post in latestPosts %}
								<a class="actualite-lien" href="{{ post.postUrl }}">
									<li class="row">
										<h6 class="col {% if post.imageUrl != '/images/default.jpg' %}col-md-8{% endif %}">
											{{ post.post_title }}
										</h6>
										{% if post.imageUrl != '/images/default.jpg' %}
											<img src="{{ post.imageUrl }}" class="col col-md-4" />
										{% endif %}
									</li>
								</a>
							{% endfor %}
						</ul>
					</aside>
				{% endif %}
			</div>
			{% if categorie.slug == "actualites" %}
				<div class="row">
					<div class="col unt-breadcrumb">
						<a href="{{ site.link }}/liste-actualites/"><i class="fa fa-arrow-left"></i> Retour aux actualités</a>
					</div>
				</div>
			{% endif %}
		</div>
	</div><!-- /content-wrapper -->
{% endblock %}
